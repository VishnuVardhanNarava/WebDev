let imGlobal = "Im a global value";

if(true) {
    let imLocal = "Im a local value";
    var imAnotherLocal = "Im another local value"
    console.log(imGlobal);
    console.log(imLocal);
}

// console.log(imLocal);
console.log(imAnotherLocal);